import * as React from "react";

const SplashScreen = () => {
  return (
    <div className="splash-screen">
      <img src="/logo-splash.png" alt="App Logo" className="splash-logo" />
    </div>
  );
};

export default SplashScreen;
